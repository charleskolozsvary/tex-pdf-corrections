\documentclass{amsart}
%% \usepackage{hyperref}
\newtheorem{theorem}{Theorem}
\begin{document}
\begin{equation}\label{eq0}
  a^2 + b^2 = c^2
\end{equation}
\begin{equation}\label{eq1}
  e^{i\theta} = -1
\end{equation}
\begin{equation}\label{eq2}
  F + V - E = 2
\end{equation}
\begin{equation}\label{eq3}
  \sum_{v \in V}\operatorname{deg}v = |E|
\end{equation}
Now for a page which will give me the reasonable upper bound for the differences caused by {\tt \string\eqref} on a page.
\pagebreak
\begin{theorem} Not a theorem. Just a bunch of {\tt \string\eqref}s. \eqref{eq0} and \eqref{eq1} and \eqref{eq2} and \eqref{eq3} and \eqref{eq0} and \eqref{eq1} and \eqref{eq2} and \eqref{eq3} and \eqref{eq0} and \eqref{eq1} and \eqref{eq2} and \eqref{eq3} and \eqref{eq0} and \eqref{eq1} and \eqref{eq2} and \eqref{eq3} and \eqref{eq0} and \eqref{eq1} and \eqref{eq2} and \eqref{eq3} and \eqref{eq0} and \eqref{eq1} and \eqref{eq2} and \eqref{eq3} and \eqref{eq0} and \eqref{eq1} and \eqref{eq2} and \eqref{eq3} and \eqref{eq0} and \eqref{eq1} and \eqref{eq2} and \eqref{eq3} and \eqref{eq0} and \eqref{eq1} and \eqref{eq2} and \eqref{eq3} and \eqref{eq0} and \eqref{eq1} and \eqref{eq2} and \eqref{eq3} and \eqref{eq0} and \eqref{eq1} and \eqref{eq2} and \eqref{eq3} and \eqref{eq0} and \eqref{eq1} and \eqref{eq2} and \eqref{eq3} and \eqref{eq0} and \eqref{eq1} and \eqref{eq2} and \eqref{eq3} and \eqref{eq0} and \eqref{eq1} and \eqref{eq2} and \eqref{eq3} and \eqref{eq0} and \eqref{eq1} and \eqref{eq2} and \eqref{eq3} and \eqref{eq0} and \eqref{eq1} and \eqref{eq2} and \eqref{eq3} and \eqref{eq0} and \eqref{eq1} and \eqref{eq2} and \eqref{eq3} and \eqref{eq0} and \eqref{eq1} and \eqref{eq2} and \eqref{eq3} and \eqref{eq0} and \eqref{eq1} and \eqref{eq2} and \eqref{eq3} and \eqref{eq0} and \eqref{eq1} and \eqref{eq2} and \eqref{eq3} and \eqref{eq0} and \eqref{eq1} and \eqref{eq2} and \eqref{eq3} and \eqref{eq0} and \eqref{eq1} and \eqref{eq2} and \eqref{eq3} and \eqref{eq0} and \eqref{eq1} and \eqref{eq2} and
  \end{theorem}
\end{document}
